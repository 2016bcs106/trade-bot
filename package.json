{
  "name": "trade-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "w": "pnpm --filter worker",
    "f": "pnpm --filter frontend",
    "deploy": "pnpm --filter frontend build && cd apps/frontend && firebase deploy --only hosting"
  },
  "description": "Quantitative research and intraday prediction platform"
}
